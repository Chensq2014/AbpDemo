version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=DataProcess_Identity
      - DataProcess_DB=DataProcess_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
